import { Meta } from '@storybook/blocks'
import { TokensGrids } from '../../Components/TokensGrids.tsx'
import { fontWeights } from '@ignite-ui/tokens'

<Meta title='Tokens/font Weights'/>

# font-Weights

Essas são os spaces usados no projeto do Ignite UI.

<TokensGrids tokens={fontWeights} hasRemValue />